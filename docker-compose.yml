version: "3.8"

services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
    command: ["./main"]
